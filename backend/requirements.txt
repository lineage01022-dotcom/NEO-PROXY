# NEO PROXY — backend runtime dependencies
# Pinned to versions verified working on Python 3.10+ / Linux.

fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2

# Async MongoDB driver
motor==3.3.1
pymongo==4.5.0

# Auth
bcrypt==4.1.3
PyJWT==2.8.0

# HTTP client for proxy probing + agent calls
httpx==0.28.1

# Validation
pydantic==2.9.2
email-validator==2.1.1

# .env loader (must be present — server.py uses it)
python-dotenv==1.0.1

# multipart form data (FastAPI file uploads etc.)
python-multipart==0.0.9
